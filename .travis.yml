language: java
before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

before_script:
  - npm install gulp -g
  - npm install bower -g

script:
  cd src/hawkengine \
  && ./build.sh

jdk:
  - oraclejdk8
