'use strict';

angular
    .module('hawk')
    .factory('jsonService', ['$http', '$q', '$state', 'CONSTANTS', function ($http, $q, $state, CONSTANTS) {

        var json;

        jsonService.createJson = function () {
            
        };

        return json;
    }]);